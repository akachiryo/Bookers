<body>
  <section>
    <p><strong>Title:</strong> <%= @book.title %></p>
    <p><strong>Body:</strong> <%= @book.body %></p>
  </section>
  <%= link_to "Edit", "/books/#{@book.id}/edit" %>
  <p class="show_p">|</p>
  <%= link_to "Back", "/index" %>
</body>

 